-- Migration 11 Down: Remove Learning Platform Tables

-- Remove indexes first
REMOVE INDEX IF EXISTS artifact_type_idx ON TABLE artifact;
REMOVE INDEX IF EXISTS artifact_notebook_idx ON TABLE artifact;
REMOVE INDEX IF EXISTS podcast_notebook_idx ON TABLE podcast;
REMOVE INDEX IF EXISTS quiz_notebook_idx ON TABLE quiz;

-- Remove tables
REMOVE TABLE IF EXISTS artifact;
REMOVE TABLE IF EXISTS podcast;
REMOVE TABLE IF EXISTS quiz;
