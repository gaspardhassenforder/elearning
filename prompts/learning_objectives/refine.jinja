You are refining a set of learning objectives based on an admin instruction.

CURRENT LEARNING OBJECTIVES:
{% for obj in objectives %}
{{ loop.index }}. "{{ obj.text }}" (id: {{ obj.id }})
{% endfor %}

INSTRUCTION: {{ refinement_prompt }}

Apply the instruction to improve, rewrite, add, remove, or reorder the objectives.
Keep objectives measurable and action-verb based (e.g., "Explain", "Apply", "Analyze").

RULES:
- To KEEP or MODIFY an existing objective: preserve its original "id" field exactly as given
- To ADD a new objective: set "id" to null
- To REMOVE an objective: simply omit it from the output array
- Keep all objectives you don't explicitly want to remove
- Return ONLY the JSON array, nothing else

Return ONLY a valid JSON array with ALL objectives (kept, modified, and new), no other text.

Example output format:
[
  {
    "id": "learning_objective:abc123",
    "text": "Explain the core concepts of machine learning and their real-world applications"
  },
  {
    "id": null,
    "text": "Design a supervised learning pipeline for a real-world classification problem"
  }
]
